[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sugar-emit-python-hypothesis"
version = "0.1.0"
description = "PEP 1.7.0 Python Hypothesis emitter kit: materializes neutral predicates as native property tests"
license = { text = "MIT OR Apache-2.0" }
requires-python = ">=3.11"
dependencies = ["blake3>=1.0.0", "hypothesis>=6.0.0"]

[project.scripts]
sugar-emit-python-hypothesis = "sugar_emit_python_hypothesis.__main__:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
