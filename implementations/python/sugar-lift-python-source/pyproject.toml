[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sugar-lift-python-source"
version = "0.1.0"
description = "Sugar Python source-language lifter over a python: operation algebra."
license = { text = "MIT OR Apache-2.0" }
requires-python = ">=3.10"
authors = [{ name = "Sugar" }]
dependencies = [
    "blake3>=0.3.0",
]

[project.optional-dependencies]
test = ["pytest>=7"]

[project.scripts]
sugar-lift-python-source = "sugar_lift_python_source.rpc:main"
sugar-bind-lift-python = "sugar_lift_python_source.bind_rpc:main"
sugar-lift-python-verify = "sugar_lift_python_source.verify_rpc:main"

[tool.setuptools.packages.find]
where = ["src"]
