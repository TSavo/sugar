[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sugar-node-membrane"
version = "0.1.0"
description = "The node membrane (#5940): source text in, constructed graph out. Standalone; stdlib-only."
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
