{
  "assertion_multiset_cid": "blake3-512:ee0c6d92a4aa44f2f43bb657b70d9b87ba202392a6ea5c2aee519197e02aa59f6a3a425b49f7aa00191688e57c48bb795b58f74b35a2518cb6d4465eeb000a50",
  "silent": 0,
  "unaccounted": -184,
  "discharged": 5810,
  "refused": 371,
  "unclassified": 320,
  "prev_unclassified": 346,
  "note": "CallsiteSugar -- call-site inlining AS a Sugar (lift-and-replace of the R7 procedural inlining block into the typed hierarchy) PLUS the nested-helper DRAIN. (i) MIGRATION (count-neutral): the old `else if resolve_inlinable_helper_call(..)` arm in `collect_assertion_entries` is replaced by one decompose-dispatch arm -> `sugar::callsite::CallsiteSugar::{decompose,desugar}` (own file src/sugar/callsite.rs, `mod sugar { mod callsite }`). `desugar` beta-reduces (param := closed-arg actual) and re-digs the substituted body through the SAME collector hierarchy in scratch buffers, committing (`Dug(InlineCommit)`) iff the body adds ZERO unclassified, else bailing (`Bail(BailCause)`). The procedural copy is DELETED -- one engine, not parallel. Confirmed count-neutral on its own (5706/371/346/-104 reproduced before the drain). (ii) THE DRAIN: the dominant blocked shape is a helper defined INSIDE a `#[test]` fn body (`fn test<T>(x: T){..} test(0u32);`), invisible to the global ReductionCtx (top-level fns only). `decompose` now resolves the helper from the LOCALLY-VISIBLE nested fns (`local_fns`) first via `resolve_inlinable_helper_call_scoped`, then the global reducer; and the nested-fn-DEFINITION refusal in `collect_assertion_entries` is now DEFERRED to after the block loop, emitted only for inner fns NOT in `reduced_helpers` (block-local Pass-2) so a drained helper is not double-counted (discharged at the callsite AND refused at the definition). SOUND: lexical scope, same monotonic `added_unclassified == 0` gate, exact-or-bail. (iii) RESULT: `reachable only via call-site inlining` 112 -> 85 (drained 7 helpers: case/check/check_fmt/do_test/f/f32_exhaustive_equivalence_test/ptr_swap_nonoverlapping_is_untyped_inner, bodies fully dig at closed sites). unclassified 346 -> 320 (-26). discharged 5706 -> 5812 (+106 SOUND inlining inflation: a helper inlined at N call sites discharges N point-wise instances -- inflation, accounted by unaccounted -104 -> -184, genuinely-unreached SILENT held at 0). refused 371 held (no fake-refuse). The 85 residual STAY honestly unclassified: census shows all 5 reached-but-blocked helpers (test/test_chain/test_mut/test_boolean_property/test_case_mapping) bail on UnsupportedConstruct (a body assert under a for/branch construct the dig does not yet enter point-wise -- e.g. `for i in 0..len { assert_eq!(v[i], ..) }` with iterator-advance / index reads); the rest are never reached as carryable closed-arg bare calls (called in macro-arg position / generic-T / runtime params). EFFECT-OR-LEAVE held: a pure-but-untranslated / branch-partitioned body STAYS unclassified, never fake-dug, never fake-refused (adversarial tests). CID conserved (classification changed, never assert sites); SILENT 0. prev_unclassified 346. HOST NOTE: this pin is the CANONICAL hermetic snapshot, produced on the Linux CI host (battleaxe, rust 1.96.0 coretests, `coretests_sweep --rustc-cfg`): pre-change baseline 5704 -> after 5810 discharged. The macOS dev host reports +2 discharged (5706 -> 5812) due to a `rustc --print cfg` target drift in discharged ONLY (target_os/atomics differ), present BEFORE this change and orthogonal to it; the gate exact-matches the CI host, so discharged is pinned to 5810, NOT the macOS 5812. The drain DELTA (discharged +106, unclassified -26, unaccounted -80, refused 0, CID held) is host-invariant. Verified: check-coretests-invariants.py passes against the battleaxe sweep output."
}
