{
  "assertion_multiset_cid": "blake3-512:ee0c6d92a4aa44f2f43bb657b70d9b87ba202392a6ea5c2aee519197e02aa59f6a3a425b49f7aa00191688e57c48bb795b58f74b35a2518cb6d4465eeb000a50",
  "silent": 0,
  "unaccounted": -104,
  "discharged": 5704,
  "refused": 371,
  "unclassified": 346,
  "prev_unclassified": 356,
  "note": "The SideEffect half -- the typed BAIL, mirror of `trait Sugar`. (i) `trait SideEffect { fn reason(&self) -> String; fn boundary(&self) -> SourceMemento; }` -- a NAMED, WARRANTED order-loss boundary retyping the untyped `None`+reason-string bail. Catalog: MutationEffect (`+=`/`&mut`/`.push`/assignment in a closure body), IterAdvanceEffect (`iter.next()`/`nth` advance), OpaqueRuntimeEffect (runtime/opaque receiver or effectful accessor, bin-2), TlsEffect (`thread_local!` `.with`), IoEffect (`write`/`send` sink), TemporalReadEffect (read of a provably-`mut` container `a[i]`). `SourceMemento` is the bail-side rope (mirror of the dig-side `Warrant`). `enum Outcome { Dug(Desugared), Hit(Box<dyn SideEffect>) }` documents the typed-both-ways shape. The two HALF-2 closure-method emit sites + the index-read emit site now MINT the bail FROM the typed effect (`effect.reason()`), so the wire format the collector emits is unchanged -- the typed machinery is INERT w.r.t. the counts (confirmed: negating the drain clause reproduces baseline 5700/359/356 EXACTLY, dig path byte-identical). (ii) THE DRAIN: `mutable container is not temporally stable` (the index-READ sibling of the whitelisted `temporally unstable`; emitted ONLY under the `mut` oracle's `is_mut_local`, so it can refuse only a genuinely-mutable read) added to the terminal whitelist as TemporalReadEffect -- 4 effect-shaped cases unclassified -> refused. EFFECT-OR-LEAVE held: a PURE-but-untranslated term (`1i32 as f64`) STAYS UNCLASSIFIED (adversarial test), never fake-refused. SOUNDNESS-COUPLING (reviewer must bless): the monotonic inlining gate (`added_unclassified == 0`) reads `refusal_disposition`, so the drain unblocks 6 previously-blocked helper inlinings -> discharged 5700 -> 5704 (+4 SOUND inlining-unblock, no fake-discharge), refused 359 -> 371, unclassified 356 -> 346, `reachable only via call-site inlining` 118 -> 112, unaccounted -98 -> -104. CID conserved (classification changed, never assert sites); SILENT 0. prev_unclassified 356. This pin is the ACTUALLY-PRODUCED snapshot per the gate contract ('predict its effect and pin it; reality is checked against your claim')."
}
