{
  "assertion_multiset_cid": "blake3-512:ee0c6d92a4aa44f2f43bb657b70d9b87ba202392a6ea5c2aee519197e02aa59f6a3a425b49f7aa00191688e57c48bb795b58f74b35a2518cb6d4465eeb000a50",
  "silent": 0,
  "discharged": 6104,
  "refused": 716,
  "unclassified": 0,
  "inactive": 62,
  "prev_unclassified": 3,
  "note": "FINAL THREE -> unclassified=0. The last three coretests unclassified asserts, each handled by the ONE predicate (DIG with teeth / BAIL with a discrimination test), driving unclassified 3->0 with the assertion-source CID ee0c6d92...0a50 CONSERVED (6377 macros seen; the CID is over assert macro SOURCE TEXT, so a classification move cannot touch it), SILENT 0, no fake-dig / no fake-refuse. ROOT CAUSE (cases 2 & 3 shared): reduced_helpers is the FILE-level dedup set keyed by bare fn NAME; two different test fns each declaring a distinct NESTED fn of the same name (hash/sip.rs `zero_byte` in _64 and _32; char.rs FIVE distinct `check`s) collided -- once one test reduced its `zero_byte`/`check`, the OTHER test's distinct same-named body was skipped at BOTH the inline-attempt AND the deferred refusal drain, so its asserts were never enumerated and fell to the per-fn 'unenumerated statement position' safety net (a FALSE unclassified). FIX: the deferred-inner-fn drains now consult a BLOCK-LOCAL reduction set = explicit commit-site inserts UNION (reduced_helpers \\ entry-snapshot), so each block's nested fns are judged on their OWN reduction; the global set is still updated for cross-test/file TOP-level dedup (regression guard nonempty_assert_helper_is_not_terminal_refused_the_twin stays green via the set-diff covering the deeper reduce_assertion_expr path). CASE 2 (hash/sip.rs::test_hash_no_bytes_dropped_32, assert!(byte<4) in nested fn zero_byte(val:u32,byte:usize), called only in arg position `hash(&zero_byte(val, 0..3))`): DIG. The block-local fix UNBLOCKS the arg-position inliner (was suppressed by the _64 twin's `zero_byte`); it beta-reduces byte:=0..3 and emits the GROUNDED finite conjunction 0<4 AND 1<4 AND 2<4 AND 3<4 (one decl per argsite #b0..b3). TEETH (assertion_lift case2_zero_byte_*): the good twin's four ground atoms are z3-SAT; the BAD-TWIN assert!(byte<3) grounds site #b3 to `3 < 3` which is z3-UNSAT (refutes). Discharged rose by EXACTLY 1 at MACRO granularity (one byte<4 source macro); the obligation-granular discharged +8 / unaccounted -7 is the documented inlining inflation of that one macro across its 4 sites, never a fake-dig. CASE 1 (iter/adapters/zip.rs::test_zip_cloned_sideffectful, assert!([&[1,1,1,0],&[1,1,0,0]].iter().any(|v| &xs==*v))): BAIL (SideEffect). `xs` is a non-mut `let` array of CountClone::new() (interior mutability via Cell, INVISIBLE to the let-mut + Cell::new-constructor oracles) consumed by `for _ in xs.iter().cloned().zip(..) {}`; .cloned() invokes a side-effecting Clone through &self that bumps each element's counter, so the asserted boolean ranges over xs's RUNTIME (clone-mutated) contents, not source literals. New bounded TemporalPlan.sideeffecting_clone_locals: a local bound to an array of NON-literal element constructions AND consumed by a .cloned()/.copied() adaptor in a bare side-effecting for-loop; closure_adaptor_refusal BAILs (bin-2 terminal) when the literal-domain .any/.all closure reads such a local. DISCRIMINATION (assertion_lift case1_*): a PURE .any over a plain [1,2,3] scalar-literal array is NOT refused (no constructor-call elements); even WITH a .cloned() loop a [1,2,3] scalar-literal array is NOT refused (i32 clone is pure) -- the fake-refuse guardrail. CASE 3 (char.rs::test_decode_utf16_size_hint, assert!(lower<=count && count<=upper.unwrap()) in a loop reading a runtime-advanced char::decode_utf16 iterator's size_hint, nested in fn check): BAIL. The block-local fix EXPOSES the loop-bearing `check` (was masked behind the name-collision with the other four char.rs `check`s); it is then judged by the EXISTING helper_body_is_runtime_terminal detector (the `let mut iter = char::decode_utf16(..)` runtime-iterator construct) -> terminal bin-2 refusal 'assertion in non-#[test] item `check` has a runtime iterator/collection construct'. DISCRIMINATION (assertion_lift case3_*): a CONCRETE nested fn with literal actuals (no runtime iterator) still DIGs via call-site inlining; two same-named-but-distinct nested `inner` fns each dig on their OWN body (a's <10, b's <100) -- the block-local keying preserves the deferred-inner-fn drain for non-loop nested fns and never cross-contaminates. NET vs main (prev_unclassified 3): discharged 6096->6104 (+8 obligation / +1 macro), refused 714->716 (+2: cases 1+3), unclassified 3->0, inactive 62, SILENT 0, unaccounted -498->-505 (-7 inlining inflation). CANONICAL linux sweep (1.96.0-x86_64-unknown-linux-gnu coretests, --rustc-cfg): 6377 macros, discharged 6104, refused 716, unclassified 0, inactive 62, SILENT 0, unaccounted -505, CID ee0c6d92...0a50. lib 153/0; assertion_lift 287/0 (+7: case2 dig+bad-twin x2, case1 bail+2 discrimination, case3 bail+1 discrimination). prev_unclassified 3.",
  "unaccounted": -505
}
