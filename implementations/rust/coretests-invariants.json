{
  "assertion_multiset_cid": "blake3-512:ee0c6d92a4aa44f2f43bb657b70d9b87ba202392a6ea5c2aee519197e02aa59f6a3a425b49f7aa00191688e57c48bb795b58f74b35a2518cb6d4465eeb000a50",
  "silent": 0,
  "discharged": 6090,
  "refused": 714,
  "unclassified": 9,
  "inactive": 62,
  "prev_unclassified": 12,
  "note": "FinalTail: two zip.rs runtime-mutated-receiver refusals, EARNED by the mut oracle, no fake-discharge. (A) `assert!(a.starts_with(&[1,2,3]))` where `let mut a = Vec::new()` is push-mutated by a side-effecting `.map(|n|{a.push(n);..})` driven by `iter.next()` (iter/adapters/zip.rs::test_zip_next_back_side_effects_exhausted, ::test_zip_nth_back_side_effects_exhausted): the slice `starts_with` over a provably-MUTABLE-local receiver is bin-2 (runtime data, not constructed from source literals). EARNED ONLY under scope.is_mut_local; an immutable receiver with a string-literal pattern still DIGS, an immutable receiver with a slice pattern STAYS the unclassified 'needs a string/char literal pattern' reason. +2 refused. (B) `assert!([&[..],&[..]].iter().any(|v| &xs==*v))` where `let mut xs=[0;6]` is mutated by `xs.iter_mut().map(|x|*x+=1)` (iter/adapters/zip.rs::test_zip_map_sideffectful): a literal-DOMAIN .any whose closure BODY reads a provably-MUTABLE-local capture is bin-2 (same proven order-loss as the for-context RUNTIME-BODY-READ terminal). EARNED ONLY when is_mut_local proves a non-closure-param capture is mutable; a PURE body still discharges via the forall lifter, an IMMUTABLE capture stays unclassified bin-1. +1 refused. NO-FAKE-DIG: discharged held at 6090 (zero new digs -- these are unclassified->refused moves, never a discharge). Net vs main(12): refused 711->714 (+3), unclassified 12->9 (-3: 2 starts_with + 1 any), SILENT 0, unaccounted -498, CID ee0c6d92...0a50 CONSERVED (assertion source-site multiset unchanged; 6377 macros seen). REMAINING 9 unclassified are honest roadmap, each needing point-wise machinery not built in this pass: 6x assert_eq! .map().try_fold(7,f)==.try_fold(7,f) over a literal range (map.rs/enumerate.rs/filter_map.rs) -- expr==expr with NO literal anchor, a real dig needs a closed try_fold/try_rfold + checked_add Option-short-circuit evaluator to ground BOTH sides to Option<i32> literals; lifting as congruent opaque terms would be always-SAT (the #2178 fake-dig trap), so LEFT. 1x assert!(...any(|v| &xs==*v)) over a literal array where `let xs` is NOT mut but its CountClone elements' counts are mutated via shared-ref method during .cloned() (zip.rs::test_zip_cloned_sideffectful) -- interior mutability the mut oracle (let-mut + mut-borrow keyed) does not see; refusing would be a fake-refuse, needs interior-mutability tracking, so LEFT. 1x assert!(byte<4) precondition inside nested fn zero_byte(val:u32,byte:usize) (hash/sip.rs::test_hash_no_bytes_dropped_32) -- reachable only via call-site inlining with literal args 0..3, genuinely diggable in principle (the 64-bit twin's path exists), so this is in-scope WORK not a source property; refusing would be a fake-refuse, LEFT. 1x assert!(lower<=count && count<=upper.unwrap()) inside a loop over a runtime-advanced char::decode_utf16 iterator reading size_hint, nested in fn check (char.rs::test_decode_utf16_size_hint) -- a documented intended 'loop over a runtime-advanced iterator' terminal (lib.rs:332) that statement_position_terminal_effect already detects, but the assert sits inside a nested fn item the structured walk does not enter, so it falls to the unenumerated safety net; needs the safety net to consult the loop-advance detector on nested-fn bodies (perturbs the deferred-inner-fn drain), LEFT to avoid an unscoped risk in this pass. lib 147/0 (+6 discrimination tests: starts_with mut-receiver positive + 2 guards; any mut-capture positive + 2 guards), assertion_lift 267/0. prev_unclassified 12.",
  "unaccounted": -498
}
