{
  "assertion_multiset_cid": "blake3-512:ee0c6d92a4aa44f2f43bb657b70d9b87ba202392a6ea5c2aee519197e02aa59f6a3a425b49f7aa00191688e57c48bb795b58f74b35a2518cb6d4465eeb000a50",
  "silent": 0,
  "discharged": 6106,
  "refused": 714,
  "unclassified": 0,
  "inactive": 62,
  "prev_unclassified": 0,
  "note": "FILTER_MAP COMPOSABLE ADAPTOR -> discharged 6104->6106 (+2). New `src/sugar/filter_map.rs` (`FilterMapSugar`) + a `(\"filter_map\", 1)` arm in `peel_fold_adaptors`: the `.filter_map(|x| <opt>)` adaptor is now a composable DECORATOR `Sugar` over the inner literal-domain seq, the exact mirror of the `filter_map` arm already proven diggable in the closed `try_fold` value-evaluator (`try_fold_eval::eval_seq_chain`). It const-evaluates the `Option`-returning closure over each element via a new crate-level `const_eval_option_closure`/`const_eval_option_expr` (same canonical `const_eval` floor as `MapSugar`'s `const_eval_unary_closure`; `Some(v)` kept with its mapped const value, `None` dropped, opaque element / non-const / runtime closure -> BAIL), materializes each kept value back to an `Expr` for EUF translation, and feeds the kept+mapped sequence through the existing fold/rfold cursor machinery. DIG SITE: `coretests/tests/iter/adapters/filter_map.rs::test_filter_map_fold` -- `xs.iter().filter_map(|&x| if x%2==0 { Some(x*x) } else { None })` threaded through `.fold(0, |i,x| { assert_eq!(x, ys[i]); i+1 })` and the `.rfold(ys.len(), |i,x| { assert_eq!(x, ys[i-1]); i-1 })` twin. The filter_map keeps the even-squared sequence [0,4,16,36,64]=ys; each cursor step's `ys[i]` resolves to the EXACT literal element, so the two source asserts (one fold, one rfold) DIG to finite conjunctions of literal pairs. CID ee0c6d92...0a50 CONSERVED (6377 macros seen; the CID is over assert macro SOURCE TEXT, so reclassifying a refused->discharged macro cannot touch it), SILENT 0, unclassified 0, no fake-dig. TEETH (assertion_lift filter_map_*): the good twins' ground equalities (0==0 .. 64==64) are z3-SAT; the BAD-TWIN asserts the kept-mapped elements against a WRONG `ys=[99;5]`, lifting to refutable (0==99 .. 64==99) pairs carrying the REAL element -> z3-UNSAT (refutes); the OPAQUE-element twin (a runtime `let xs = make_xs()` receiver) BAILS (refused as a runtime/opaque source property, never fake-dug). NET vs main floor (prev_unclassified 0): discharged 6104->6106 (+2), refused 716->714 (-2), unclassified 0 (unchanged), inactive 62, SILENT 0, unaccounted -505 (unchanged). SKIPPED-NO-DEMAND (no fold/rfold/for_each/for-loop terminal over a FINITE LITERAL DOMAIN in coretests -- a decorator would be a dead node): flat_map (all fold/rfold uses `let mut it` advanced by `.next()`/`.next_back()` before the fold = runtime iterator advance, and the for-loop use has an infinite `(x..).step_by(1).take(3)` base; try_fold uses a Range-returning let-bound closure ref, not the decorator path); scan (only `.size_hint()` + a named-fn `scan(0, add)` collect); step_by (only `.collect()`/`.size_hint()` + a for-loop over a RUNTIME `step` variable); map_while (only two `.size_hint()` asserts); inspect (all `inspect(|_| n += 1)` side-effecting closures feeding `.collect()`/`.count()`, plus a `Result::inspect` -- side-effecting, would bail). CANONICAL linux sweep (1.96.0-x86_64-unknown-linux-gnu coretests, --rustc-cfg): 6377 macros, discharged 6106, refused 714, unclassified 0, inactive 62, SILENT 0, unaccounted -505, CID ee0c6d92...0a50. lib 166/0; assertion_lift 295/0 (+4 filter_map: cursor-fold dig + cursor-rfold dig + bad-twin z3-UNSAT + opaque-element bail). prev_unclassified 0. NOTE: this branch (feat/iter-adaptor-coverage) is internally consistent against its OWN sweep; a concurrent FormatSugar branch also moves these counts, so T does the final combined re-pin at merge.",
  "unaccounted": -505
}
