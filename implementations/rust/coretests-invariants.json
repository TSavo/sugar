{
  "assertion_multiset_cid": "blake3-512:ee0c6d92a4aa44f2f43bb657b70d9b87ba202392a6ea5c2aee519197e02aa59f6a3a425b49f7aa00191688e57c48bb795b58f74b35a2518cb6d4465eeb000a50",
  "silent": 0,
  "discharged": 6240,
  "refused": 685,
  "unclassified": 33,
  "prev_unclassified": 42,
  "note": "MatcherGapDig on main(42): fix the macro_rules! matcher + guard the runtime-local expansion. TWO matcher fixes: (1) match_prefix now RECURSES into a metavar-bearing delimiter group (`($valid:expr,$invalid:expr)`) instead of failing structural token_tree_eq on length -- matches rust's matcher exactly (gated on matcher_group_has_metavar so a literal `()` group still matches by equality; inner match must consume the WHOLE input group). (2) match_repetition BACKTRACKS a just-consumed separator when the next round does not match, so a TRAILING `,` is left for `$(,)?` instead of failing the match (the `assert_chunks!(.., (..), (..),)` shape). These make `assert_chunks!`/`search_asserts!` EXPAND. SOUNDNESS guard: an expansion that binds a RUNTIME ITERATOR/SEARCHER local (`let mut iter = $s.utf8_chunks(); let chunk = iter.next()` / `$n.into_searcher($h)`) is bin-2 runtime data; lifting its asserts EUF-coalesces the per-invocation local onto ONE var so a distinct literal per sibling invocation makes the merged invariant UNSAT (ex-falso) -- a fake-discharge. So try_macro_expansion_entries REFUSES such expansions (expansion_binds_runtime_iterator_local). Net: the 9 str_lossy assert_chunks! invocations move unclassified('no rule matched')->refused('runtime iterator local'); no rule matched 9->0. The pattern.rs search_asserts! (38 atoms) would have been fake-discharges (UNSAT/vacuous EUF over a runtime searcher) -- guarded out; not in the assertion denominator (non-assert-named macro), no SILENT leak. starts_with bucket (2): NOT dug -- `a.starts_with(&[1,2,3])` in zip.rs is a runtime-mutated Vec receiver compared to a SLICE literal (not a str), correct BAIL; extending starts_with to slice patterns would be a fake-dig (string prefix-of over non-string). discharged 6240 UNCHANGED (no fake-dig), refused 676->685 (+9), unclassified 42->33 (-9), SILENT 0, CID ee0c6d92...0a50 CONSERVED. lib 139/0 (16 macro_expand matcher tests), assertion_lift 266/0. prev_unclassified 42.",
  "unaccounted": -641
}
