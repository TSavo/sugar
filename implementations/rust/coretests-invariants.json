{
  "assertion_multiset_cid": "blake3-512:ee0c6d92a4aa44f2f43bb657b70d9b87ba202392a6ea5c2aee519197e02aa59f6a3a425b49f7aa00191688e57c48bb795b58f74b35a2518cb6d4465eeb000a50",
  "silent": 0,
  "discharged": 6106,
  "refused": 714,
  "unclassified": 0,
  "inactive": 62,
  "prev_unclassified": 0,
  "note": "REBASE COMBINED RE-PIN (feat/format-sugar onto origin/main @ #2196): FormatSugar + FilterMapSugar are DISJOINT features, rebased clean (no manual conflict resolution). FORMATSUGAR is a ZERO-DRIFT SOUNDNESS UPGRADE, count-conserving: the ~407 `format!` rows that already discharged via the OPAQUE `macro:format!(..)` EUF fake-dig now ground to TEETH'D `eq(str_const(computed), str_const(expected))` by RUNNING the lifter's own stdlib formatter over written literals (recompute-don't-reimplement); a wrong expected string is z3-UNSAT. Runtime/opaque args BAIL to the conservative opaque path (`Ok(None)`); unformattable cases take a NAMED refusal (`Err`). `src/sugar/format.rs` (`FormatSugar`) is now the single authority for string-formatting sugar and SUBSUMES + DELETES `src/flt2dec_eval.rs` (the four `core::num::imp::flt2dec` test-helper surfaces are `format!` renderings of a closed float; recognition/reconstruction/render now OWNED by `sugar::format`, lib.rs keeps ONLY the dispatch call to `flt2dec_helper_mode`/`lift_flt2dec_helper`). FILTER_MAP (#2196, already on the main floor this rebase sits atop): `src/sugar/filter_map.rs` (`FilterMapSugar`) + a `(\"filter_map\", 1)` arm in `peel_fold_adaptors` const-evaluate the Option-returning closure over each literal-domain element (Some kept+mapped, None dropped, opaque/non-const/runtime -> BAIL) and feed the kept sequence through the existing fold/rfold cursor machinery; this is the +2 already baked into the 6106 base. BOTH features confirmed present post-rebase: `pub mod format;` + `pub mod filter_map;` both declared, the format dispatch hook and the filter_map peel arm both live, `flt2dec_eval.rs` deleted, `filter_map.rs` present. CID ee0c6d92...0a50 CONSERVED across the upgrade (6377 assert macros seen; the CID is over assert-macro SOURCE TEXT, and FormatSugar reclassifies a discharged-row's BACKING from opaque-EUF to str_const teeth WITHOUT touching the source corpus, so the multiset is identical), SILENT 0, unclassified 0, no fake-dig. CANONICAL combined linux sweep (1.96.0-x86_64-unknown-linux-gnu coretests, --rustc-cfg): 6377 macros, discharged 6106 (95.8%), refused 714 (11.2%), unclassified 0, inactive 62, SILENT 0, unaccounted -505, CID ee0c6d92...0a50. Combined tests on battleaxe: --lib 185/0; --test assertion_lift 302/0 (FormatSugar T-FORMAT teeth/bad-twins + filter_map cursor-fold/rfold dig + bad-twin z3-UNSAT + opaque-element bail; both test sets kept). prev_unclassified 0.",
  "unaccounted": -505
}
