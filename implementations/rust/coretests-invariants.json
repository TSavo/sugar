{
  "assertion_multiset_cid": "blake3-512:ee0c6d92a4aa44f2f43bb657b70d9b87ba202392a6ea5c2aee519197e02aa59f6a3a425b49f7aa00191688e57c48bb795b58f74b35a2518cb6d4465eeb000a50",
  "silent": 0,
  "unaccounted": -369,
  "discharged": 5940,
  "refused": 624,
  "unclassified": 122,
  "prev_unclassified": 143,
  "note": "REFUSE wave (runtime residue: impl-method / runtime if-guard / runtime expr-stmt). The Hit side of Outcome{Dug|Hit} -- a runtime/un-nameable value is a NAMED terminal Effect (refused), accounted not silent. CLASSIFICATION-ONLY (no assert site added/removed/edited): CID CONSERVED (ee0c...000a50), SILENT 0, discharged HELD 5940 (I refuse, I do not dig -- NO fake-discharge). Hermetic battleaxe CI-host sweep, rust 1.96.0 coretests, coretests_sweep --rustc-cfg. prev_unclassified 143. lib 121/0 (incl. 5 new tests), assertion_lift 233/0.\n\nBEFORE -> AFTER: discharged 5940 -> 5940 (held); refused 603 -> 624 (+21); unclassified 143 -> 122 (-21); unaccounted -369 (held -- the assertion multiset is byte-identical, only the disposition of 21 reasons changed); SILENT 0; CID unchanged.\n\nPER-CAUSE NAMED terminal Effects (detection-EARNED, whitelisted Disposition::Refused). (A) IMPL-METHOD = +17 (ImplMethodEffect: 'assertion in an impl method, reachable only at runtime when the method is invoked'). An assert in an impl method BODY runs only when the method is INVOKED, observing the receiver's RUNTIME state -- no single timeless `t`. STRUCTURAL detection (the assert is lexically inside a method body): 6 from the top-level Item::Impl path (iter/adapters/mod.rs `next`/`next_back` reading `self.done`/`self.exhausted`; map_windows.rs `check` reading `self.dropped_twice.load(SeqCst)`/`self.alive_count.load(SeqCst)`) + 11 from nested `impl` STATEMENTS inside test fns surfaced at the expr-stmt site (fmt/float.rs `ExactExpWriter::finish`+`write_str` x8 reading per-call accumulators `self.prefix_pos`/`self.total_len`; ptr.rs `ThinBox::new` `layout.size()` + `TaggedPointer::set_tag` reading runtime `data` x2; hint.rs `Drop::drop` reading `self.cell.get()` x1). (B) RUNTIME EXPR-STMT = +4 (RuntimeExprStmtEffect: 'runtime expression-statement'): macros.rs::temporary_scope_introduction's borrow/drop-scoping tuples `(assert_matches!(*MutRefWithDrop(&mut val).0, 0), std::mem::take(&mut val))` -- the asserted value is read through a `&mut` borrow with a sibling `mem::take` mutation of the same local, mutably aliased, no single timeless `t`. EARNED by runtime_expr_statement_effect (a `&mut`/assignment signal in the statement).\n\nHONEST NON-DRAIN (the fake-refuse guard, the inverse sin -- never refuse a computable case to zero the count): the IF-CONTEXT bucket (7) STAYS unclassified. Read against LIVE source, ALL 7 corpus if-guards are CONST/cfg, not runtime: bool.rs::test_bool_not `if !false`/`if !true` (literal-bool guards, x4); iter/traits/step.rs `if cfg!(target_pointer_width = \"64\")` (x1); fmt/mod.rs::test_fmt_pointer `if cfg!(target_pointer_width = \"32\")` (x2). A const/cfg/literal guard is COMPUTABLE-but-unimplemented (ConditionalSugar bailed on lifting a `!false` literal or a `cfg!` macro), NOT a runtime cause -- refusing it would be FAKE-REFUSE. if_guard_is_runtime explicitly excludes `cfg!(..)` (a compile-time target predicate) and a bare literal, firing only on a positive `&mut`/assignment signal in the GUARD. So the if-context detector is ARMED (a `&mut`-borrow guard WOULD refuse, IfGuardRuntimeEffect) but EARNS zero corpus refusals -- 7 stay unclassified, the honest disposition.\n\nDISCRIMINATION (both directions, adversarially tested): assertion_in_impl_method_is_refused_runtime_reachability + assertion_in_nested_impl_statement_is_refused_runtime_reachability + runtime_expr_statement_through_mut_borrow_is_refused + runtime_if_guard_over_mut_borrow_is_refused [REFUSE with the SPECIFIC named cause]; const_if_guard_and_pure_expr_statement_stay_unclassified_not_fake_refused [a `!false`/`cfg!(..)` if-guard and a pure (no-&mut) expr-statement STAY unclassified, never fake-refused]. refusal_disposition_is_a_terminal_whitelist extended with the 3 new terminal reasons + the bare 'under if context' kept on the UNCLASSIFIED side.\n\nEFFECT-OR-LEAVE held: only a DETECTED runtime cause is terminalized; a const/cfg/literal/pure shape stays unclassified, never fake-refused. HOST NOTE: canonical battleaxe CI-host snapshot; macOS dev host may report +2 discharged (rustc --print cfg drift, orthogonal); the gate exact-matches the CI host.\n\n--- prior wave (DIG+REFUSE: COMPLETE bin-1 for-context classification) ---\nDIG+REFUSE wave (COMPLETE bin-1 for-context classification): literal-body -> DIG (the cursor index-read unroll), runtime-body/endpoint/accumulator -> REFUSE (NAMED terminal Effect, the Hit side of Outcome{Dug|Hit}). CID CONSERVED (ee0c...000a50 -- no assert site added/removed/edited), SILENT 0 (genuinely-unreached). Hermetic battleaxe CI-host sweep, rust 1.96.0 coretests, coretests_sweep --rustc-cfg. prev_unclassified 249. lib 116/0, assertion_lift 233/0.\n\nBEFORE -> AFTER: discharged 5864 -> 5940 (+76); refused 398 -> 603 (+205); unclassified 249 -> 143 (-106); unaccounted (net) -194 -> -369; SILENT 0; CID unchanged. NO discharged REGRESSION (verified per-file: zero files lost a discharged assert -- nothing previously-discharged became refused).\n\nREFUSE-HALF -- per-cause NAMED terminal Effects (detection-EARNED in for_context_refusal_reason, whitelisted Disposition::Refused; the DIG fires FIRST, this is the ELSE after desugar declines AND a runtime cause is detected): (A) RUNTIME DOMAIN ENDPOINT = 150 (`for i in 0..v.len()` / `for c in start..key` -- the universe is a runtime quantity, not a finite construction from source literals; for_domain_endpoint_is_runtime requires both range endpoints to const_int, else runtime). (B) RUNTIME BODY READ = 1 (literal domain but the body asserts read OPAQUE/temporally-unstable/mutable-container runtime data; EARNED by the body's OWN refusal reason). (C) RUNTIME-VALUED ACCUMULATOR = 20 (a mutated builder / non-int / `let mut cur` accumulator, NOT a simple `acc += <const>` counter; EARNED by loop_body_mutates && !loop_mutation_is_simple_counter_only). 171 total NAMED runtime-cause refusals.\n\nThe 48 DIRECT bin-1 for-context unclassified buckets went to 0 (literal array 16->0, array-but-opaque 1->0, literal range 31->0). The 171 NAMED refusals EXCEED the 48 direct because the SAME runtime causes appear INSIDE call-site-inlined helpers (unicode.rs test_boolean_property/test_case_mapping `for c in start..*range.start()` runtime-char-endpoint + runtime lookup(c); chain.rs test_chain `for i in 0..xs.len()` runtime-endpoint + iter.advance_by). When those helpers inline, their for-loops now get the NAMED terminal, so the monotonic inlining gate (added_unclassified==0 reads refusal_disposition) lets the blocked inlines COMMIT -- the call-site-tail 'reachable only via call-site inlining' bucket dropped 85 -> 27 (-58). Some helpers commit-as-REFUSED (unicode.rs +45 refused: every body assert is a runtime lookup) and some commit-as-DISCHARGED (chain.rs +40, slice.rs +36 = the +76: the helpers' STRAIGHT-LINE asserts -- `let mut iter = xs.iter().chain(ys); assert_eq!(iter.advance_by(len), Ok(()))` -- lift as SOUND uninterpreted EUF obligations, byte-identical to the top-level dig path, the established opaque-subterm-is-a-sound-obligation doctrine; they were always EUF-liftable and were only BLOCKED by the unclassified for-loop sibling). This is the SAME gate-coupling cascade the mutable-container whitelist documented (+4 discharged then). NOT a fake-discharge: the inline desugar commits ONLY when added_unclassified==0 and the lifted form is byte-identical; the consistency pass checks every EUF obligation. The -369 unaccounted (net) is the inlining N-obligations-per-source-assert accounting artifact (same sign/mechanism as the prior -184->-194), NOT a silent drop -- SILENT (genuinely-unreached) is 0.\n\nFAKE-REFUSE GUARD (the inverse of fake-dig, equally forbidden -- never refuse a computable case to zero the count): a computable-but-unimplemented body STAYS unclassified. int_log.rs ilog10_u* (`for i in 0..=K { let p = (10).pow(i); if p>=10 { assert_eq!((p-1).ilog10(), i-1) } .. }`) has a literal range, NO runtime endpoint, NO runtime body refusal, NO non-counter mutation -- it is in-scope WORK (a let-SSA + conditional over the loop var, no lifter yet), so for_context_refusal_reason returns the existing UNCLASSIFIED 'not unconditional point-wise' reason (143 unclassified remain). Adversarially tested both directions: forloop_runtime_endpoint_refuses_with_named_domain_effect / forloop_runtime_body_read_refuses_with_named_body_effect / forloop_runtime_valued_accumulator_refuses_with_named_accum_effect [REFUSE with the SPECIFIC named cause], forloop_simple_counter_is_not_refused_as_runtime_accumulator + forloop_computable_but_unimplemented_body_stays_unclassified [STAY unclassified, never fake-refused].\n\nHOST NOTE: canonical battleaxe CI-host snapshot; macOS dev host may report +2 discharged (rustc --print cfg drift, orthogonal); the gate exact-matches the CI host.\n\n--- prior wave (DIG: for-loop-body cursor index-read unroll, 0 corpus drain) ---\nDIG wave (SOUND INFRA, 0 corpus drain -- an honest non-drain beats a fake-dig): for-loop-body cursor -- index-read unroll over a LITERAL-INT range. CID conserved (ee0c...000a50 -- no assert site added/removed/edited), SILENT 0, discharged held 5864, refused held 398, unclassified held 249. Hermetic battleaxe CI-host sweep, rust 1.96.0 coretests, coretests_sweep --rustc-cfg. prev_unclassified 249. lib 116/0, assertion_lift 228/0 (incl. 5 NEW adversarial discrimination tests: forloop_index_read_over_literal_range_digs_exact_elements [positive: for i in 0..3 { v[i]==ys[i] } digs (10,10),(20,20),(30,30)], forloop_index_read_bad_twin_is_refutable_carrying_real_elements [TEETH: v!=ys -> z3-UNSAT pairs (10,99),(20,99),(30,99) carrying BOTH real elements, not the always-SAT index(v,k)==index(ys,k) EUF], forloop_simple_counter_step_over_literal_range_is_not_dug_as_universal [BAIL guardrail #1: a `sum += v[i]` mutated accumulator is not fake-resolved to its final literal], forloop_index_read_over_runtime_length_range_is_not_unrolled [BAIL guardrail #3: 0..v.len() runtime endpoint lifts as a FORALL, never unrolled to literal pairs], forloop_index_read_over_mutable_array_stays_uninterpreted_not_dug [BAIL guardrail #2: a `let mut buf` index read stays the EUF accessor]).\n\nMECHANISM. `for i in 0..n { assert_eq!(v[i], ys[i]) }` over a LITERAL-INT range and IMMUTABLE literal v/ys is the value-in-scope dig applied to the loop BODY (the same shape `FoldSugar` digs for cursor folds, now for the bare for-loop). lift_bounded_forall's BoundedDomain::Range branch, when BOTH endpoints const-fold to literal ints (term_as_int) AND a resolvable literal array is in scope, UNROLLS the range to the finite conjunction body[i:=0] AND .. AND body[i:=n-1] -- each i a concrete num(k) -- then resolve_index_in_formula rewrites each index(v,k)/index(ys,k) read to its concrete literal element (REUSING the FoldSugar machinery: capture_literal_arrays from let_inits at decompose time, mut-gated + translated in ForAllSugar::desugar, resolve_index_in_formula over the threaded literal index). TEETH: the asserted equality then carries the REAL element values on both sides (10==99), so a wrong-expected twin is z3-REFUTABLE -- not the always-SAT uninterpreted index(v,k)==index(ys,k) EUF a fake-dig would leave.\n\n  GUARDRAILS (exact-or-bail, all adversarially tested): (1) SIMPLE-COUNTER -- a body that MUTATES an accumulator (`sum += <const>`) is gated by loop_body_mutates at the top of lift_bounded_forall (the loop is not lifted as a universal; the post-loop assert reading the mutated accumulator is NOT threaded by the body lifter -- it stays unclassified, never fake-resolved). (2) INDEX over IMMUTABLE LITERAL array only -- the mut-gate in ForAllSugar::desugar (is_mut_local) excludes a `let mut arr`; a mutable/unknown array read stays the uninterpreted EUF accessor (the established floor). (3) LITERAL domain only -- a runtime endpoint (0..v.len()) has no literal count (term_as_int None), so the unroll declines and the loop falls to the guarded-forall path (body lifts as a sound EUF obligation, NEVER unrolled to a hallucinated count). (4) ADDITIVE -- the unroll engages ONLY when a resolvable literal array is in scope (!literal_arrays.is_empty()); a plain `for x in 0..3 { g(x) }` with no array read stays the guarded forall it always was, so no existing-discharged loop's lifted FOL form changes (CID/discharged conserved; bounded_for_loop_lifts_as_forall still passes).\n\nWHY 0 CORPUS DRAIN (verified against LIVE source, like DigCursor caught a fabrication -- the reason-classifier over-estimated): the rust 1.96.0 coretests corpus has NO soundly-diggable for-loop-body accumulator OR index-read. Every literal-domain for-loop body reads RUNTIME values: fmt/mod.rs (`let mut formatting_options` + runtime accessors fmt.flags()&1, get_sign()), iter/traits/iterator.rs (`0..v.len()` RUNTIME endpoint + `let mut iter; iter.advance_by(i)` + v.iter().nth(i) runtime methods), num/flt2dec/{dragon,grisu}.rs (mutated non-int accumulators prevpow10/prevtenk + runtime fns mul_pow10/cached_power/max_pow10_no_more_than), ascii.rs (0..501 if-guard + runtime String to_ascii_uppercase; 0..=0x1F MUTATES buf via index-assign; 128..=255 runtime from_u8), ascii_char.rs (0..128 write! mutation), slice.rs (0..16/0..4 memrchr/align_to over let mut), bignum.rs (0..8*3 NON-literal endpoint + runtime Big methods), int_log.rs (1..=100_000 runtime checked_ilog10 + f32 cast), num/floats.rs (0..64 runtime float .powi). The accumulators that exist (accum.rs cnt+=1, cloned.rs count+=1, iter/mod.rs x+=1) are all .map(closure-side-effect) consumed by a runtime fold (.sum/.product/().extend), NOT for-loop bodies. The CALLSITE-TAIL (85) is likewise NOT blocked on a liftable literal-domain for/index body: chain.rs::test_chain inlines to `let mut iter = ..chain(..); iter.advance_by(i)` (iterator-advance EFFECT over a 0..xs.len() runtime-length loop); num/mod.rs::test_num<T> is GENERIC (runtime trait dispatch ten.add(two)) -- both bin-2 runtime, not the brief's hypothesized literal-domain shape. Digging any would be a FAKE-DISCHARGE (guardrail violation). The mechanism is built + adversarially proven + ready for a real target; it correctly drains zero from this corpus. EFFECT-OR-LEAVE held: only a value provably reducible to a literal element over a statically-determinable literal index would be dug.\n\nHOST NOTE: canonical battleaxe CI-host snapshot; macOS dev host may report +2 discharged (rustc --print cfg drift, orthogonal); the gate exact-matches the CI host.\n\n--- prior wave (DIG: versioned-cursor accumulator-init + bin-1 body-lift over LITERAL domains) ---\nDIG wave: versioned-cursor accumulator-init + bin-1 body-lift over LITERAL domains (the VALUE-IN-SCOPE half of the let-initializer iterator-cursor folds). CID conserved (ee0c...000a50 -- no assert site added/removed/edited), SILENT 0, refused held 398. Hermetic battleaxe CI-host sweep, rust 1.96.0 coretests, coretests_sweep --rustc-cfg. unclassified 256 -> 249 (-7); discharged 5857 -> 5864 (+7); refused unchanged. prev_unclassified 256. lib 116/0, assertion_lift 223/0 (incl. 4 NEW adversarial discrimination tests: cursor_fold_rfold_over_literal_array_digs_exact_elements [positive], cursor_fold_bad_twin_is_refutable_carrying_real_elements [TEETH: bad twin -> z3-UNSAT (6==99) carrying the real element], cursor_fold_skip_while_deref_predicate_digs [positive], cursor_fold_over_runtime_indexed_array_stays_unclassified_not_dug [BAIL guardrail]).\n\nMECHANISM. The let-init iterator-cursor folds (iter/adapters/{skip,filter,enumerate,filter_map,skip_while}.rs *_fold) are `<lit-array>.iter().<adaptor>().fold/rfold(<init>, |acc, &x| { assert_eq!(x, ys[acc..]); .. })` over LITERAL xs/ys. The defolder (decompose_fold -> FoldSugar) ALREADY threads the accumulator over the finite element sequence and digs the in-place case; it declined these on (a) the fold ACC-INIT being `ys.len()` / `xs.len() - 1` -- a literal-array length is not an int LITERAL, so const_int(init) returned None and the whole defold bailed to the 'let-initializer' refusal; and (b) the skip_while/filter predicate `|&x| *x < 15` -- the `*x` deref was outside const_eval's closed set.\n  FIX (3 surgical, all EXACT-OR-BAIL):\n  (1) const_int_acc_init(expr, let_inits): a SCOPE-AWARE acc-init resolver -- int literal | `<lit-array>.len()` (-> element count, resolved through let_inits via literal_array_len_with_lets) | `+`/`-`/`*` arithmetic over those (`xs.len() - 1`). THE LAW: over a literal domain the cursor START position is itself a literal in scope. A runtime `.len()` / non-arithmetic op returns None -> the defold declines (safe under-claim). Wired into decompose_fold's acc_0 only.\n  (2) const_eval `*x` (UnOp::Deref): the const model holds scalar VALUES (no pointers) and a `&x`/`&&x` param binds the dereferenced element, so `*x` is the identity on that value -- faithful, lets the skip_while/filter predicate const-evaluate. (+2: the two skip_while.rs `|&x| *x<15` cursor folds.)\n  (3) resolve_index_in_formula (TEETH): after the accumulator (the index) is threaded to a concrete literal, a body `index(ys, <const>)` over a CAPTURED literal array (FoldSugar.literal_arrays, owned from let_inits at decompose time) is rewritten to its concrete element TERM (with const_fold_int_term folding `sub(4,1)` -> 3 first). This is what makes a wrong-expected twin z3-REFUTABLE: the asserted RHS carries the REAL element value (`6==99`) instead of the always-SAT uninterpreted EUF accessor (`6==index(ys,3)`). SCOPED to the fold body -- no other already-discharged site's lifted form changes (CID/refused both held; the index resolution is a localized strengthening, OOB/unknown-array index left as the EUF floor).\n\n+7 = +5 (acc-init `.len()` resolution: skip.rs x2 rfold, filter.rs x1 rfold, enumerate.rs x1 rfold[xs.len()-1], filter_map.rs x1 rfold) + +2 (skip_while.rs `*x` deref predicate). The forward `.fold(0, ..)` cursor cases already dug on main (acc-init was the literal 0).\n\nHONESTLY NOT DRAINED (correct disposition, NOT forced -- an honest non-drain beats a fake-dig): the OTHER bin-1 'opportunity' buckets are NOT soundly diggable and STAY refused/unclassified -- fmt/mod.rs (body asserts read RUNTIME accessors `fmt.flags()&1`, loop var only on the RHS), iter/traits/iterator.rs (`v.iter().nth(i)` runtime iterator method + `let mut iter; iter.advance_by(i)` intervening mutation), num/flt2dec/{dragon,grisu}.rs (mutated accumulator `prevpow10`/`prevtenk` + runtime fns), the try_fold(f) comparisons (runtime fn `f`), zip.rs/cloned.rs `.any(|v| &xs==*v)` (compared `xs`/`count` are runtime). filter_map's *_fold stays bin-2 (filter_map Option const-eval is the established inexact-adaptor bail). These are runtime-value or un-nameable-cursor cases -- digging them would be a FAKE-DISCHARGE. EFFECT-OR-LEAVE held: only a value provably reducible to a literal element over a statically-determinable cursor was dug.\n\nHOST NOTE: canonical battleaxe CI-host snapshot; macOS dev host may report +2 discharged (rustc --print cfg drift, orthogonal); the gate exact-matches the CI host.\n\n--- prior wave (StmtPos qualified-continue-path) ---\nStmtPos qualified-continue-path wave (value-NOT-in-scope half of the statement-position buckets). Classification-only; CID conserved (ee0c...000a50 -- no assert site added/removed/edited), SILENT 0, discharged held 5857 (NO fake-dig). Hermetic battleaxe CI-host sweep, rust 1.96.0 coretests, coretests_sweep --rustc-cfg. unclassified 272 -> 256 (-16); refused 372 -> 398 (+26); discharged unchanged. prev_unclassified 272. lib 116/0, assertion_lift 219/0 (incl. 4 new adversarial discrimination tests: 2 refuse-direction + 2 boundary/guardrail digs).\n\nPRINCIPLE: the statement-position assert buckets split on VALUE-IN-SCOPE. The value-NOT-in-scope cases (the asserted value flows through a runtime continuation with no nameable literal/version) are SOUND TERMINAL refusals naming the qualified continue path -- the SAME class as the existing bin-2 'runtime data, not constructed from source literals' terminals. EARNED by detecting the continuation structurally, never assigned by position: a statement carrying a CONSTRUCTED literal (a match over a literal scrutinee, a plain block over literals) matches NONE of the detectors and DIGS (guardrail tests both directions).\n\nQUALIFIED CONTINUE PATHS classified this wave (typed SideEffect + whitelisted in refusal_disposition):\n  (1) FUTURE CONTINUATION (+5, future.rs::test_join): an `.await` anywhere, or a free-fn `block_on(async{..})`, flows the asserted value from a runtime executor (`let x = join!(..).await; assert_eq!(x, 0)`). ControlFlowEffect. NOTE: recursing this would FAKE-DIG `x==0` as a literal (proven empirically), so the refusal is the sound disposition, not the under-claim it looked like. unclassified->refused.\n  (2) OPAQUE REFLECTION (+21, mem/type_info.rs): `match Type::of::<T>().kind { TypeKind::X(b) => assert_eq!(b.field, ..) }` reads compile-time reflection over runtime type identity (TypeId) -- not a source-literal construction. New ReflectionEffect (reflection_scrutinee detector strips `const{}`/`.kind`; fires only on a Type::of/TypeId::of/.info() scrutinee). The surviving-arm body asserts are accounted at the Match arm; the panic-locus variant-pin discharge is unchanged (so discharged held). 21 = the 11 previously-unenumerated + 10 reflection body asserts previously MASKED in the unaccounted-negative pool by the panic-locus synthetic-pin overcount -- now honestly surfaced as refused (this is the +26-vs-16 gap and the -184->-194 unaccounted shift; CID confirms no site changed, no double-count of a real site). type_info unenumerated 11->0.\n\nVALUE-IN-SCOPE half NOT drained this wave (honest unclassified, sized follow-up): the let-initializer iterator-cursor folds (iter/adapters/{skip,enumerate,skip_while,filter}.rs *_fold, 7) are over LITERAL domains (xs.iter().skip(5).fold over a literal array) -- DIGGABLE, not refusable; the defolder already digs the in-place case but declines on inner-`let` accumulator shadowing / `i-1` index exprs / `*x` deref in skip_while|filter closures / `len()-1` acc-init / pre-consumed receivers. Closing those is the versioned-cursor -> phantom-function-over-version lift (a foundational mechanism), deliberately NOT rushed here to avoid fake-digging an untracked cursor. They STAY unclassified (correct disposition: an in-scope value must not be refused). The remaining unlifted-expr (15: hint Cell+drop, ptr raw-ptr mutation, others) and the 1 residual char.rs size_hint loop (inside an un-inlined nested fn, a call-site-inlining bucket) likewise stay unclassified.\n\nEFFECT-OR-LEAVE held: only a DETECTED runtime continuation is terminalized; pure/literal positions dig or stay unclassified, never fake-dug, never fake-refused. HOST NOTE: canonical battleaxe CI-host snapshot; macOS dev host may report +2 discharged (rustc --print cfg drift, orthogonal); the gate exact-matches the CI host.\n\n--- prior wave (CONSOLIDATED Sugar-breadth: callsite + BinaryOpSugar + TermBreadthSugar + MatchSugar) ---\nBinaryOpSugar (-14): a COMPARISON in TERM position (== != < <= > >=) now lifts to the bool-valued FOL fact it states (the gap was purely the term path; translate_binary_bool_assertion already handled top-level atoms, const_eval already folded all six ops to Bool). const operands -> const-fold to a Bool literal; non-const stable operands -> a `cmp:<rel>` bool ctor (new RelationOp::cmp_ctor_name keeps eq/neq/lt/le/gt/ge DISTINCT, so `cmp:lt(x,y)` and `cmp:gt(x,y)` cannot both be true -- teeth preserved, no fake-coalesce that operator_call_name's Eq/Ne collapse would have caused). Negated comparison routes to the atom path before the term fallback (regression-guarded). Drains `bool.rs` (`false == false`) and `iter/traits/iterator.rs` (`a[0] < b[0]`) `unsupported term operator` rows. Structural BAIL: comparison over a `mut` container operand still refuses.\n\nTermBreadthSugar (-34): three term-path sub-fixes in translate_term_in_scope. (A) FLOAT CASTS: scalar_cast_type_key recognizes f16/f32/f64/f128, so `expr as f32` emits opaque EUF `cast:f32(<expr>)` (IEEE width in the ctor NAME only, no Real semantics); drains num/mod.rs float<->float round-trips incl. the two `infinity as f32` rows (cast receiver now lifts inside translate_infinity_eq_assertion). `neg_zero as f32 == -0.0f32` stays unclassified (signed-zero IEEE guard, untouched). (B) UNSAFE-BLOCK TRANSPARENCY: single-tail `unsafe { e }` / `{ e }` is value-transparent (unsafe is a compile-time obligation, not a value transform) -- unwrap-and-recurse; `unsafe { ok.unwrap_unchecked() }`/`*c1.as_ptr()`/`align_of_val_raw(b)` lift congruent to bare form. Only the WRAPPER is transparent: `unsafe { &mut *cell.get() }` still bails. (C) TRY/ASYNC -> TERMINAL refuse (+1): `try{}`/`async{}`/`?` in term position is effectful control-flow (new ControlFlowEffect, whitelisted terminal in refusal_disposition); drains the future.rs join!-over-try row unclassified -> refused.\n\nMatchSugar (0 drain, sound infra): match scrut { pat_i => A_i } lifts as conj_i(guard_i => conj(A_i)) -- literal arm => scrut==lit, qualified/wrapper variant => variant_of(scrut)==\"variant::<tag>\", final _ => not(or(prior guards)); BAILS on binding/guard/or/range/struct-binding patterns, effectful/non-translatable scrutinee, mutating body, or any arm assert that does not fully lift. The 7 coretests `match context` sites do NOT drain (all payload-binding arms / loop-variable / opaque scrutinees -- draining needs sound payload-binding substitution, scoped out to avoid fake-discharge), so this is composing infrastructure, not a numbers drain; honestly so. (The 2 stale `assert_in_match_arm_is_refused` / `ordinary_match_without_panic_arm_stays_refused` tests were reconciled: the former now lifts a literal-arm match as a guarded implication, the latter stays a clean bail.)\n\nCASCADE (leaves drive wrappers): the leaf term fixes (binop/termbreadth) also let some bin-1 for-context-over-literal bodies lift point-wise -- the `for context over a literal range` bucket dropped 44 -> 31. The residual 272 roadmap is dominated by call-site-inlining tail (85), bin-1 for/adaptor bodies (31+16+6+3), statement-position (20+12+7), and assorted float/macro/helper/predicate buckets. EFFECT-OR-LEAVE held throughout: pure-but-untranslated or branch-partitioned bodies STAY unclassified, never fake-dug, never fake-refused (adversarial twins verified per class). HOST NOTE: canonical battleaxe CI-host snapshot; macOS dev host reports +2 discharged (rustc --print cfg target_os/atomics drift, orthogonal); the gate exact-matches the CI host."
}
