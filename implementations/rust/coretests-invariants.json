{
  "assertion_multiset_cid": "blake3-512:ee0c6d92a4aa44f2f43bb657b70d9b87ba202392a6ea5c2aee519197e02aa59f6a3a425b49f7aa00191688e57c48bb795b58f74b35a2518cb6d4465eeb000a50",
  "silent": 0,
  "unaccounted": -98,
  "discharged": 5700,
  "refused": 359,
  "unclassified": 356,
  "prev_unclassified": 357,
  "note": "HERMETIC snapshot, gated in CI. This push: the typed Sugar HIERARCHY. (i) The procedural defolder + for_each/for-loop forall machinery were ported into their typed home -- a `trait Sugar { desugar(&self, ctx) -> Option<Desugared> }` composite tree (Interpreter pattern; Option = typed BAIL). `decompose_*` builds the tree (node + decomposed children -> inner Sugars); `desugar()` walks inward until `LiteralSugar` bottoms out or a layer returns None. Classes: Desugared = (value, warrant) = Seq(Vec<DesugaredElem>) (the literal floor; each elem its source Expr glued to its ConstVal-when-exact; synthetic-but-warranted under an adaptor) | Constraints{atom,n,warrant}; LiteralSugar (base); AdaptorSugar over Adaptor (Identity/Rev/Enumerate/Filter/Map/Skip/Take/SkipWhile/TakeWhile, one EXACT-OR-BAIL arm each); FoldSugar/RFoldSugar (acc threaded const-folded, full-closure-body purity gate); ForAllSugar (for-loop / .for_each, shares lift_bounded_forall; ForEachSugar = acc=() case); ConditionalSugar (the claim-side atom, guard => claim, NEVER bare claim). Pure refactor verified by IDENTICAL numbers (5699/359/357/-98, SILENT 0, CID conserved); dead try_lift_*/apply_adaptors free fns removed (~430 lines). (ii) ConditionalSugar wired into the if-context: `if guard { then } [else { else }]` -> `(guard => then) and (not guard => else)`, the guard lifted via the SAME translate_bool_assertion path an assert!(guard) takes (so opaque-but-pure guards lift faithfully via EUF; a side-effecting/mutated-state guard or a non-fully-lifting/mutating branch BAILS -- exact-or-bail; temporal stability inherited from the assert path). Drains the genuinely-guarded if-context cases (composes inside for-loop bodies via ForAllSugar): discharged +1 (5699->5700), unclassified -1 (357->356). The for-context literal bucket is dominated by UNCONDITIONAL bodies asserting over runtime/opaque data (string ops, runtime indexing, opaque calls) -- correctly left unclassified, never fake-dug. EXACT-OR-BAIL verified adversarially: a guarded assert lifts as `guard => claim` (Implies present) not bare claim; an else branch lifts under the negated guard; a side-effecting guard / mutating branch BAILS; a wrong-claim literal-array guarded loop lifts to a REFUTABLE conjunction carrying the actual element values under the guard. CID conserved (we change classification, never the assert sites); SILENT 0. prev_unclassified 357. Drive -> 0."
}
