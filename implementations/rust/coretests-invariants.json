{
  "assertion_multiset_cid": "blake3-512:ee0c6d92a4aa44f2f43bb657b70d9b87ba202392a6ea5c2aee519197e02aa59f6a3a425b49f7aa00191688e57c48bb795b58f74b35a2518cb6d4465eeb000a50",
  "silent": 0,
  "discharged": 6096,
  "refused": 714,
  "unclassified": 3,
  "inactive": 62,
  "prev_unclassified": 9,
  "note": "FoldEval: closed try_fold/try_rfold VALUE evaluator (src/try_fold_eval.rs) digs the 6 iter/adapters/{map,enumerate,filter_map}.rs `assert_eq!(<chain>.try_fold(7,f), <chain>.try_fold(7,f))` rows that were the #2178-trap roadmap. MECHANISM: a `<closed-literal-range>.{map|enumerate|filter_map}().try_fold(<lit>, <pure T::checked_* closure>)` operand is a FINITE construction from source literals, so the evaluator symbolically executes the chain + the Option-accumulator fold (?-short-circuit, width-clamped checked arithmetic at the DECLARED int width) EXACT-OR-BAIL, and materializes the result to a `Some(n)`/`None` literal Expr. translate_term_in_scope grounds that through the ORDINARY ctor path (`Some(11250)` -> Ctor call:Some[Int(11250)]), so `assert_eq!(LHS,RHS)` becomes a GROUNDED `call:eq:Some[call:Some[a], call:Some[b]]`. TEETH (per-case bad-twin, all z3-UNSAT, in assertion_lift try_fold_*_bad_twin_refutes): map6 Some(11250)==Some(11250) good / Some(11250)==Some(12274) twin(RHS init 7->8); map7 Some(18431) good / unequal twin; enum100 Some(7379) good / unequal twin(RHS pair i+9->i+8); enum101 Some(7961) good / unequal twin; fm32 Some(9194) good / unequal twin(RHS 2*x->2*x+1); fm33 Some(23556) good / unequal twin. EVALUATOR-VALIDATION (lib try_fold_eval::tests, 6 tests): every materialized ABSOLUTE value pinned against the real-Rust hand-computed result (Some(11250)/Some(18431)/Some(7379)/Some(7961)/Some(9194)/Some(23556)), plus a width-clamp test (i8 overflow->None vs i32->Some) and a runtime-mut-iterator BAIL test. SOUNDNESS: the evaluator is gated on !is_mut_local; a `let mut iter; iter.try_fold(0,..)` (the runtime rows map.rs:10/12, enumerate.rs:105/107, filter_map.rs:36/38) BAILS and falls through to its existing classification UNCHANGED -- no reclassification, no fabricated Some for an advanced iterator (assertion_lift runtime_iterator_try_fold_is_not_grounded confirms the opaque method:try_fold stays paired with the real call:None, never a grounded Some). let_bindings are recorded INCREMENTALLY per statement (shadowing-correct: enumerate.rs's second `let f` does not leak back to rows 100/101). NO-FAKE-DIG: discharged rose by EXACTLY 6 (one per proven refuting bad-twin). Net vs main(prev_unclassified 9): discharged 6090->6096 (+6), unclassified 9->3 (-6), refused 714 (UNCHANGED), inactive 62, SILENT 0, unaccounted -498, CID ee0c6d92...0a50 CONSERVED (assertion source-site multiset unchanged; 6377 macros seen -- the CID is over assert macro SOURCE TEXT, not the lifted formula, so a classification move cannot touch it). REMAINING 3 unclassified are honest roadmap, each needing machinery not built here: 1x assert!(...any(|v| &xs==*v)) over a literal array whose CountClone elements' counts mutate via shared-ref during .cloned() (zip.rs::test_zip_cloned_sideffectful) -- interior mutability the let-mut+mut-borrow oracle does not see; refusing would be a fake-refuse, LEFT. 1x assert!(byte<4) inside nested fn zero_byte(val,byte) (hash/sip.rs::test_hash_no_bytes_dropped_32) -- reachable only via call-site inlining with literal args 0..3, in-scope WORK, LEFT. 1x assert!(lower<=count && count<=upper.unwrap()) inside a loop over a runtime-advanced char::decode_utf16 iterator reading size_hint, nested in fn check (char.rs::test_decode_utf16_size_hint) -- a loop-over-runtime-advanced-iterator terminal the structured walk does not enter (nested fn item), LEFT to avoid an unscoped safety-net change. lib 153/0 (+6 evaluator-validation tests), assertion_lift 280/0 (+13: 6 positive + 6 bad-twin discrimination + 1 runtime-not-grounded structural). prev_unclassified 9.",
  "unaccounted": -498
}
