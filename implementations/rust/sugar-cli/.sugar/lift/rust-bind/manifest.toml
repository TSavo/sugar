# SPDX-License-Identifier: MIT OR Apache-2.0
#
# PEP 1.7.0 `kind = "lift"` plugin manifest for the Rust bind-IR surface.
#
# When `sugar` is invoked from this crate root
# (implementations/rust/sugar-cli/), the bind-lift dispatcher resolves
# `surface = "rust-bind"` to this manifest and spawns the configured
# command. `working_dir = "."` anchors `command` at this crate root, so
# `../target/debug/sugar-walk-rpc` resolves to the workspace target
# at implementations/rust/target/debug/.
name = "rust-bind-lift"
command = ["../target/debug/sugar-walk-rpc", "--rpc"]
working_dir = "."
