# Sugar CI: the acid test
#
# Drop sugar into a project, prove correctness with zero code changes.
# `make ci` runs the two suites that actually do that: `test-rust` (the rust
# workspace, including the crate-pair inheritance E2E, exercising the ts
# / python realize kits over RPC) and `test-python` (the python kit, including
# the numpy proof). No federation conformance gate; the Makefile is the
# contract.

name: CI

on:
  push:
    branches:
      - main
  pull_request:

# Cancel superseded runs on the same branch / PR.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  actions: read
  attestations: write
  id-token: write

jobs:
  prepare:
    name: shared Rust build
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 180

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Rust build owner
        uses: ./.github/actions/setup-rust-cache
        with:
          mode: seed

      - name: Check apt installed-package fast path
        run: tools/test-ci-apt-install.sh

      - name: Install sugarbin hashing dependency
        run: tools/ci-apt-install.sh b3sum

      - name: Build shared Rust artifacts once
        run: |
          for b in sugar sugar-lift rust_test_assertions_rpc coretests_sweep; do
            bin/sugarbin --profile release --bin "$b" >/dev/null
          done
          for b in sugar sugar-ir-smt-lib sugar-ir-lean sugar-ir-coq sugar-ir-maude sugar-walk-rpc rust_test_assertions_rpc witness_rpc discharge_cli; do
            bin/sugarbin --profile debug --bin "$b" >/dev/null
          done

  core:
    name: core (${{ matrix.name }})
    needs: prepare
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: refusal vocabulary
            target: check-lift-refusal-vocabulary
            python: false
            z3: false
            b3sum: false
          - name: Python format
            target: test-python-format
            python: true
            z3: false
            b3sum: false
          - name: claim-mass tripwires
            target: test-claim-mass-tripwires
            python: true
            z3: false
            b3sum: false
          - name: self attestation
            target: self-attest
            python: false
            z3: false
            b3sum: true
          # coretests-source-audit stays `make coretests-source-audit` (instrument /
          # R-measuring stick). Default CI excludes it while unresolved R remains
          # nonzero by construction; do not re-gate until R is driven to 0.
          - name: coretests invariants
            target: coretests-invariants
            python: false
            z3: false
            b3sum: true

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Consume shared Rust build
        uses: ./.github/actions/setup-rust-cache
        with:
          mode: consume

      - name: Check Rust formatting
        if: matrix.target == 'check-lift-refusal-vocabulary'
        working-directory: implementations/rust
        run: cargo fmt --all -- --check

      - name: Set up Python 3.12
        if: matrix.python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install lift package deps
        if: matrix.python
        run: python -m pip install --quiet blake3 pynacl cbor2 pytest

      - name: Install source-audit system deps
        if: matrix.z3
        run: tools/ci-apt-install.sh z3 b3sum

      - name: Install sugarbin hashing dependency
        if: matrix.b3sum && !matrix.z3
        run: tools/ci-apt-install.sh b3sum

      - name: Run core instrument
        run: make ${{ matrix.target }}
        env:
          CI: '1'
          GH_TOKEN: ${{ github.token }}

      - name: Attest published sugar binary
        if: matrix.target == 'self-attest' && github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/attest-build-provenance@v4
        with:
          subject-path: implementations/rust/target/release/sugar

  showcases:
    name: showcases (${{ matrix.shard }}/4)
    needs: prepare
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        shard: [0, 1, 2, 3]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Consume shared Rust build
        uses: ./.github/actions/setup-rust-cache
        with:
          mode: consume

      - name: Set up Python 3.12
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install lift package deps
        run: |
          python -m pip install --quiet \
            blake3 pynacl cbor2 pytest \
            -e implementations/python/sugar-build-witness \
            -e implementations/python/sugar-lift-py-tests \
            -e implementations/python/sugar-lift-python-source \
            -e implementations/python/sugar-lift-py-pytest-witness

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Install system deps
        run: |
          tools/ci-apt-install.sh z3 cvc5 maven unzip b3sum
          if ! command -v vampire >/dev/null 2>&1; then
            curl -fsSL -o /tmp/vampire.zip https://github.com/vprover/vampire/releases/download/v5.0.1/vampire-Linux-X64.zip
            unzip -o -q /tmp/vampire.zip -d /tmp/vampire-bin
            sudo install -m 0755 /tmp/vampire-bin/vampire /usr/local/bin/vampire
          fi

      - name: Run showcase shard
        run: make test-showcases
        env:
          CI: '1'
          GH_TOKEN: ${{ github.token }}
          SHOWCASE_SHARD_COUNT: '4'
          SHOWCASE_SHARD_INDEX: ${{ matrix.shard }}

  acid-test:
    name: acid test (make ci)
    # Aggregate genuine success/failure results, but do not manufacture a red
    # test result when concurrency intentionally supersedes the whole run.
    if: ${{ always() && !cancelled() }}
    needs: [prepare, core, showcases]
    runs-on: ubuntu-latest
    steps:
      - name: Report the complete acid-test vector
        env:
          PREPARE_RESULT: ${{ needs.prepare.result }}
          CORE_RESULT: ${{ needs.core.result }}
          SHOWCASE_RESULT: ${{ needs.showcases.result }}
        run: |
          echo "prepare=$PREPARE_RESULT core=$CORE_RESULT showcases=$SHOWCASE_RESULT"
          test "$PREPARE_RESULT" = success
          test "$CORE_RESULT" = success
          test "$SHOWCASE_RESULT" = success
