# recensus4: examples-gate clean re-bucket (2026-07-06)

Part of #3677 / #3686 criterion 10.

## Input

Clean observation run on battleaxe, current main, `smoke` suite, 62 examples:

- `.out/examples-gate-summary-recensus4.json`
- `.out/examples-gate-logs-recensus4/*.log`

Raw counts at capture time: 15 GREEN, 34 `unclassified/example-failure`, 6
`prove-refused/provenance-kind-required`, 3 `prove-refused/panic-callsite-row`,
2 `durable-row-missing/scientific-python-showcase`, 2 `mint-missing/*`,
singletons.

**runner-env measured at ZERO this run.** No row in the recensus4 summary
matched `RUNNER_ENV_PATTERNS` (venv-permission-denied,
native-extension-load-failure, missing-module-in-temp-venv). The venv
self-heal + classifier landed in #3689 held; every red row below is real
product drift, not box noise.

## What changed in `tools/examples_gate.py`

Added 9 new `FAILURE_PATTERNS` entries (pattern additions only; the
runner-env refusal logic in `write_expectations_from_summary` and
`RUNNER_ENV_PATTERNS` were not touched):

| shape | what it catches |
|---|---|
| `prove-missing/java-kit-consistency-row-absent` | Java kit prove step emits no consistency/witness row at all (`MISSING`, not a verdict) |
| `durable-verify-mismatch/consistency-refused-at-durable` | prove discharges the witness-package but durable re-verify recomputes consistency rows as refused |
| `toolchain-plan/witness-memento-not-loaded` | pinned toolchain plan stays `declared`, no witness-memento fulfilled it |
| `prove-red/bodyguard-claims-failed-or-undecided` | claims JSON report itself carries nonzero failed/undecided counts |
| `harness-bug/edge-for-stopiteration` | harness script raises `StopIteration` in `edge_for()` |
| `factory-gap/rust-coretests-composite-for-loop` | rust factory has no Composite recognizer for a for-loop AST shape |
| `verdict-drift/witness-refusal-expected-got-discharged` | bad-suite witness discharges when refusal was expected |
| `silent-truncation/no-terminal-verdict-after-witness-verify` | log ends silently right after the verify-witness header, no PASS/FAIL text at all (weakest signal in the file: anchors on end-of-log, not message content) |

## The 34 unclassified rows, re-bucketed

**20 -- Java kit prove-row-absent** (`prove-missing/java-kit-consistency-row-absent`,
new issue #3747): every Java showcase that mints successfully but gets no
consistency/witness row back from `prove` at all. Supersedes the stale
`prove-output/empty-json-receipt` pin (work order #3582, which predates the
#3696/#3709 sugarbin conversion and no longer matches current failure text).
Examples: java-abs-bound, java-abs-flagship, java-abs-universe,
java-assertion-consistency, java-b64-strong, java-b64-tails,
java-bound-federation, java-callbind-consistency, java-codec-universe,
java-commons-codec-crc32, java-crc32-universe, java-crc32-valuepin,
java-forall-loop, java-instance-universe, java-mt-reference,
java-pattern-regex, java-testng-consistency, java-urlsafe-seam,
java-voltron, java-witness-recompute.

**6 -- durable-verify mismatch** (`durable-verify-mismatch/consistency-refused-at-durable`,
new issue #3749): prove discharges the witness-package, but a separate
durable-artifact re-verify pass recomputes the consistency rows as refused.
This is the predicted vacuity-family landing spot, but the terminal FAIL
text (`FAIL[good]: durable consistency statuses [...]`, square brackets) did
not match the existing `proof-gap/urlsafe-universe-missing` pattern (which
expects `FAIL(good): ...`, parens, or the literal word "vacuous" in the last
200 lines). Two of these six -- **bitflags-showcase** and
**forall-loop-showcase** -- were previously pinned
`prove-refused/provenance-kind-required` under #3587; that pin is now stale,
their live shape has moved here and no longer shows provenance-KIND text.
Examples: bitflags-showcase, forall-loop-showcase, num-integer-showcase,
semver-showcase, url-showcase, uuid-showcase.

**3 -- tokio toolchain-plan/witness-memento gap** (`toolchain-plan/witness-memento-not-loaded`,
new issue #3750): tokio-await-implication-edge, tokio-channel-implication-edge,
tokio-mutex-implication-edge. Supersedes the stale
`build-failed/sugar-walk-bridge-source-symbol` pin (#3606); the build
succeeds now, the gap is downstream.

**1 -- std-core-bodyguard-precondition** (`prove-red/bodyguard-claims-failed-or-undecided`,
new issue #3751): claims report itself shows 2 failed + 2 undecided of 53.
Supersedes the stale `build-failed/sugar-walk-bridge-source-symbol` pin
(#3606).

**1 -- pandas-consumer-demo** (`harness-bug/edge-for-stopiteration`,
new issue #3752): **regression from a pinned GREEN row.** The harness script
itself raises `StopIteration` in `edge_for()`. Not a reclassification of an
existing red -- this example was expected to pass.

**1 -- rust-coretests-report** (`factory-gap/rust-coretests-composite-for-loop`,
new issue #3753): factory has no Composite recognizer for a tuple-destructured
`for (i, (_, w)) in witness.iter().enumerate()` shape; lift plugin then
closes stdout before responding. Supersedes the stale
`rust-lift-panic/chained-comparison` pin (#3588); that panic no longer
reproduces.

**1 -- serde-json-showcase** (`verdict-drift/witness-refusal-expected-got-discharged`,
new issue #3754): bad-suite witness discharges when the fixture expects
refusal. Supersedes the stale `build-failed/sugar-walk-bridge-source-symbol`
pin (#3606).

**1 -- tokio-effect-consistency** (`silent-truncation/no-terminal-verdict-after-witness-verify`,
new issue #3755): the script dies silently right after printing
`== verify good witness ==`, no PASS/FAIL text, no error, 45-line log. Reads
as a crash or a swallowed nonzero exit, not a text-classifiable product
verdict. Supersedes the stale `kit-transform/callsite-line-null` pin (#3607).

## The 6 provenance-kind-required rows: live, not stale

#3587 is closed, but all 6 rows pinned under
`prove-refused/provenance-kind-required` still show the exact live text in
the recensus4 sweep:

> `consistency check refused: contract ... lacks required provenance KIND
> for ambient testimony (contract memento lacks required
> proofirProvenance/sourceWarrants provenance KIND)`

Examples: build-witness-showcase, numpy-attribute-safety-showcase,
numpy-vendor, python-literal-base20, python-literal-base64,
rust-test-assertion-consistency.

**Flagging loudly per #3677/#3686 criterion 10: this reads as a genuine live
recurrence, not a stale pattern match on a newer shape.** Either #3587's fix
did not cover this code path (witness-package / build-witness / ambient
self-post testimony contracts specifically lack the provenance KIND stamp,
as opposed to whatever #3587 originally targeted), or the regression
reintroduced the gap. This may mean **#3587 needs reopening**. Kept pinned
under #3587 rather than silently re-patterned to a new shape -- these rows
are not a classifier miss, the text is identical to what #3587 described.

## GREEN rows

15 GREEN this sweep, `last_observed_rc`/`last_observed_seconds` refreshed for
all of them; `grounds`/`first_seen` left untouched for rows that were already
pinned GREEN. No new GREEN promotions among the 34 unclassified or the 6
provenance rows -- pandas-consumer-demo (previously GREEN) is the only
verdict change, and it moved the wrong direction (see above).

## Work orders filed

| issue | shape | count |
|---|---|---|
| #3747 | prove-missing/java-kit-consistency-row-absent | 20 |
| #3749 | durable-verify-mismatch/consistency-refused-at-durable | 6 |
| #3750 | toolchain-plan/witness-memento-not-loaded | 3 |
| #3751 | prove-red/bodyguard-claims-failed-or-undecided | 1 |
| #3752 | harness-bug/edge-for-stopiteration (GREEN regression) | 1 |
| #3753 | factory-gap/rust-coretests-composite-for-loop | 1 |
| #3754 | verdict-drift/witness-refusal-expected-got-discharged | 1 |
| #3755 | silent-truncation/no-terminal-verdict-after-witness-verify | 1 |
| #3587 (reused) | prove-refused/provenance-kind-required (LIVE recurrence, flagged for reopen) | 6 |

Unchanged shapes (mint-missing/forall-vampire-claim-rows #3579,
mint-missing/base64-decode-property-rows #3578,
durable-row-missing/scientific-python-showcase #3576,
prove-refused/panic-callsite-row) kept their existing work-order issues; no
new issue filed for those.
