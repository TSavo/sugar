# Corpus receipt: grounding path #3939 + #3949 + #3952

**Date:** 2026-07-09  
**HEAD under test:** `origin/main` @ `17bcdf7` (includes merge commits below)  
**Lesson:** #3924 — structural over-constraint only shows on dual-assert / corpus, not on solo PR instruments.

## Stack

| PR | Merge | Title |
| --- | --- | --- |
| #3939 | `dae1725` | Mint closed rstrip/lstrip universes and resolve module.attr to Bridge |
| #3949 | `b0f980f` | Prefer closed strip ambient; seed body dig siblings and defaulted arity |
| #3952 | `0fbb0c2` | Opt-in nested ExternalBridge so dig towers finish without logo poison |

Shared surface: `call_sugar.py`, `literal_call_report.py`, `factory_build_context.py`, strip/dig tests.

## Receipts (combined state)

### 1. Focused instruments (the three PRs)

```
pytest tests/test_nested_dig_external_bridge.py \
       tests/test_open_dig_strip_tower.py \
       tests/test_strip_literal_universe.py \
       tests/test_urlsafe_alphabet_universe.py
→ 8 passed
```

### 2. Logo twin (itsdangerous-token-padding) — dual-assert family

| Twin | `base64_encode` consistency | `int_to_bytes` |
| --- | --- | --- |
| **good** | **discharged** | discharged |
| **bad** (padded `=`) | **unsatisfied** | discharged |

Commands: `sugar mint --out .` then `sugar prove --allow-failed-components --json .` in each twin dir  
(venv: itsdangerous install; `ITSDANGEROUS_LOGO_VENV`).

This is the dual-assert / lie-catching probe for the membrane: same `#euf#` key, good RHS sat, padded RHS unsat under ambient `¬suffix-of("=", out)`.

### 3. Broader dig/bridge k-filter

```
pytest tests/ -k 'urlsafe or strip or dig or bridge or call_sugar or method_call
  or module_attr or import_target or base64 or rstrip or ExternalBridge or nested_external'
→ 234 passed, 2 failed (pre-fix); **237 passed after instrument rewrite**
```

#### Failures (combined main — **not introduced by #3939–3952 instruments**)

| Test | Expectation | Observed | Likely stream |
| --- | --- | --- | --- |
| `test_call_sugar.py::test_factory_gap_strategy_classifies_builtin_call_frontier` | `call-builtin:sum` gap | `observed == "value"` | #3918 coordinate-ize `sum` (`call:sum`) — stale gap classification |
| `test_numpy_totality_lane2.py::test_symbolic_method_call_is_registered_runtime_effect` | `Incomplete` / `FactoryGapEffect` for `SymbolicValue.astype` | `Complete(OpaqueOpCallsite(...))` | #3809 vendor-method coordinate (`OpaqueOpCallsite`) — gap→coordinate |

**Resolved in the corpus-receipt follow-up PR:** instruments rewritten to the coordinate law —

- builtin frontier probe uses still-uncoordinated `open(path)` → `call-builtin:open`
- `sum([1,2])` asserts `OpaqueOpCallsite(callee="sum")` with computed sum
- symbolic `astype` asserts `OpaqueOpCallsite` with `computed is None` (not fiat green)

### 3b. After instrument rewrite (same HEAD + this PR)

```
k-filter dig/bridge  → 237 passed, 0 failed
focused stack + call_sugar + lane2 coordinate  → 23 passed
logo twin  → GOOD discharged / BAD unsatisfied
```

### 4. Full `sugar-lift-py-tests` sweep

Started full suite on combined main; many `F` markers in the first ~80% of collection before timeout (180s). **Not bisected** to #3939–3952 vs concurrent #3809 merges. Treat as: *main is not green-corpus; membrane stacks need a named receipt, not assumption of clean suite.*

## Structural risk named (#3924 class)

What the stacked PRs *could* have over-constrained:

1. **Default `nested_external_bridge=False`** — if ever flipped on for force_floor, logo encoding-STOPs (`str.suffixof` sort). Guarded by default + tests.
2. **Module.attr → Bridge / siblings** — can widen resolve and change gap vs bridge classification (corpus items that expected gap may complete).
3. **Closed strip ambient preferred** — covers logo; open dig EUF towers must stay out of ambient mint.

What the **solo PR instruments did not catch**: the two stale gap tests above (coordinate stream). What **this receipt catches**: logo still flips; focused stack green; two related-suite reds owned by coordinate drift.

## Follow-ups (ordered)

1. **Retire or rewrite** the two stale gap tests for `call:sum` / `OpaqueOpCallsite` on main (or pin them to the new coordinate law).
2. **Check-in logo showcase receipt** under `examples/itsdangerous-token-padding` (slim run asserting GOOD discharged / BAD unsatisfied) so CI holds the dual-assert.
3. **Membrane-stack gate:** any PR that touches `call_sugar.py` + dig/bridge must attach this class of receipt (focused instruments + at least one dual-assert / logo twin), not only unit green.

## Commands to reproduce

```bash
export PYTHONPATH=implementations/python/sugar-lift-py-tests/src:implementations/python/sugar-lift-python-source/src
export ITSDANGEROUS_LOGO_VENV=...  # venv with itsdangerous
export PATH=implementations/rust/target/release:$PATH

# focused
pytest implementations/python/sugar-lift-py-tests/tests/test_nested_dig_external_bridge.py \
  implementations/python/sugar-lift-py-tests/tests/test_open_dig_strip_tower.py \
  implementations/python/sugar-lift-py-tests/tests/test_strip_literal_universe.py \
  implementations/python/sugar-lift-py-tests/tests/test_urlsafe_alphabet_universe.py -q

# logo
for twin in good bad; do
  (cd examples/itsdangerous-token-padding/$twin && sugar mint --out . && sugar prove --allow-failed-components --json .)
done
```
