# numpy showcase showdown — mint/prove/verify over one numpy operation.
#
# lift   : the numpy sugar `.proof` (in imports/) carries numpy.add's sugar;
#          mint lifts the unit-test CONTRACTS from test_numpy_add.py.
# prove  : the test contracts discharge (consistent) / refuse (contradictory).
# verify : signed witness mementos cite the discharging solver.
#
# The pytest and python-bind lift surfaces both self-resolve their `.proof`s.
# (recognize/materialize were retired; ingress is lift of native source.)
[[plugins]]
name = "python-numpy-testing-lift"
kind = "lift"
surface = "python-numpy-testing"

[[plugins]]
name = "pytest-witness-lift"
kind = "lift"
surface = "python-pytest-witness"

[[plugins]]
name = "python-bind-lift"
kind = "lift"
surface = "python-bind"

[solvers]
default = "z3"
# Dispatch each obligation class to the solver that owns its theory. Missing
# binaries simply miss the registry and report Undecidable (loudly) for that
# class -- the dispatch is the structure; install the seat to light it up.
[solvers.dispatch]
linear_arithmetic = "z3"
bitvectors = "bitwuzla"
strings = "cvc5"
equational_theory = "vampire"
dependent_type = "lean"
categorical_structure = "coq"
default = "z3"
[solvers.z3]
binary = "z3"
flags = ["-smt2", "-in"]
[solvers.vampire]
binary = "vampire"
flags = ["--input_syntax", "smtlib2"]
[solvers.lean]
binary = "lean"
[solvers.coq]
binary = "coqtop"
flags = ["-batch"]
